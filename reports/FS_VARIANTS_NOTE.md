# FS Variants Note

- Mainline remains `fragment_suppress`.
- `thresholded_fs` and `topk_fs` are isolated validation branches only.
- They do not change the current README / CURRENT_STATUS mainline claims.
- Default configs still point to `topology.variant: fragment_suppress`.
- Fast 40e experiment configs live in `configs/experiments/`.
